# clasp — contains your scriptId, and login credentials
.clasp.json
.clasprc.json

# Node (only needed if you deploy with clasp)
node_modules/
npm-debug.log*
package-lock.json

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# Local Claude Code preview scaffolding (machine-specific paths)
.claude/
